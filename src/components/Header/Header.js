import React from 'react';
import Menu from '../Menu/Menu';

// header component to show header section in every page
const Header = () => {
    return (
        <header>
            <Menu></Menu>
        </header>
    );
};

export default Header;